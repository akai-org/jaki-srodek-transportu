import distance_by_mode from './distance.js';

class DistanceInfo {
    modes = ['driving', 'walking', 'bicycling', 'transit'];
    constructor(origin, destinatoin){
        car = distance_by_mode()
    }
}